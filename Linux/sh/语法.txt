一.变量：
定义变量：
			your_name="qinjx"

使用变量：
	使用一个定义过的变量，只要在变量名前面加美元符号即可。
	例：
		echo $your_name
	
	
三.控制结构：
     (一)if语句：
      1）if语句：
          if  条件
          then   
            命令
          fi 

       2)if ……else语句：

            if 条件

            then

                  命令

            else

                 命令

             fi

         (二)for语句：

            for 条件

            do

                命令

            done

         (三)while语句：

            while

            do

                 命令

            done